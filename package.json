{
  "name": "ants",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "tsc --noEmit --watch --preserveWatchOutput & web-dev-server --watch",
    "serve": "web-dev-server --watch",
    "type": "tsc --noEmit --watch --preserveWatchOutput"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@web/dev-server": "^0.1.21",
    "@web/dev-server-esbuild": "^0.2.14",
    "typescript": "^4.3.5"
  }
}
